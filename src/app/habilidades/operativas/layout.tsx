import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Habilidades operativas — Félix Zamora',
  description: 'Habilidades operativas y administrativas de Félix Zamora.',
}

export default function OperationalSkillsLayout({ children }: { children: React.ReactNode }) {
  return children
}
